import { AsyncStorage } from 'react-native';
// LIBRARIES - REDUX
// import { createNavigationReducer } from 'react-navigation-redux-helpers';
// import AppNavigator from '@/navigators/AppNavigator';

// const nav = createNavigationReducer(AuthNavigator);

// export default nav;
